name: alerts-run
on:
  schedule:
    - cron: '*/15 * * * *'  # every 15 minutes
  workflow_dispatch:        # allows manual run

jobs:
  run-alerts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install requirements
        run: python -m pip install --upgrade pip && pip install -r requirements.txt

      - name: Run alerts
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python alerts_upgraded.py

      # OPTIONAL: commit state.json back so state persists between runs.
      # If you want state.json to be saved across runs, you'll need:
      # - a PAT secret with repo permissions, or use the default checkout token with
      #   persist-credentials: true and allow the action to push.
      # The commit step below is optional and needs careful setup; omit if unsure.
      #- name: Commit updated state.json
      #  run: |
      #    git config user.name "github-actions[bot]"
      #    git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      #    git add state.json || true
      #    git commit -m "Update state.json" || true
      #    git push || true
